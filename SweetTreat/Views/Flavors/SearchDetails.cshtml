@{
  Layout = "_Layout";
}
@using SweetTreat.Models;
@model List<SweetTreat.Models.Flavor>;

<h2>Flavor Search</h2>
<hr />

@if(Model.Count == 0)
{
  <p>No Results</p>
}

@foreach (Flavor flavor in Model) {
  <p>@flavor.FlavorName</p>
}